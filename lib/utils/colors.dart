import 'package:flutter/material.dart';

// Defining the app's color palette for easy reuse
class AppColors {
  static const Color primaryBlue = Color(0xFF3B82F6);
  static const Color lightBlue = Color(0xFFBFDBFE);
  static const Color background =
      Color(0xFFF0F4F8); // A very light, clean background
  static const Color white = Colors.white;
  static const Color accentAqua = Color(0xFF2DD4BF);
  static const Color darkText = Color(0xFF1E293B);
  static const Color lightText = Color(0xFF64748B);
  static const Color errorRed = Color(0xFFEF4444);
  static const Color secondaryBlue =
      Color(0xFF4F6D94); // Lighter blue for accents
  static const Color primaryYellow =
      Color(0xFFFDB913); // Bright yellow for buttons
}
